interface Field {
    id: number;
    name: string | undefined;
    symbol: string | undefined;
    percentage: number | undefined;
}

interface TokenItem {
    name: string,
    symbol: string,
    image: string
}